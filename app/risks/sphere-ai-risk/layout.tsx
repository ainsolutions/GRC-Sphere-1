import type React from "react"
export default function SphereAiRiskLayout({
  children,
}: {
  children: React.ReactNode
}) {
  // return <>{children}</>
  return <div className="min-h-screen">{children}</div>

}
